# pbip_analyzer has no third-party runtime dependencies -- it relies only on
# the Python 3.12+ standard library (json, re, pathlib, dataclasses, argparse,
# logging). This keeps the tool trivially embeddable inside any environment,
# including a future FastAPI backend, without dependency conflicts.

# Streamlit web UI:
streamlit>=1.36
graphviz>=0.20
plotly>=5.18
openpyxl>=3.1

# Development / testing only:
pytest>=8.0
